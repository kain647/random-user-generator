import React from 'react';
import ReactDOM from 'react-dom';
import Generator from "./random-user-generator";

ReactDOM.render(
  <React.StrictMode>
    <Generator />
  </React.StrictMode>,
  document.getElementById('root')
);
